import React, {Component} from 'react';

export class SideBar extends Component {
  render () {
    return (
      <div>
        <button>Load More Jokes</button>
      </div>
    );
  }
}

export default SideBar;
